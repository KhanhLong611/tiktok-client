const routes = {
  home: '/',
  following: '/following',
  profile: '/profile/:id',
  favorite: '/profile/:id/favorites',
  upload: '/upload',
  search: '/search',
  explore: '/explore',
  live: '/live',
  video: '/:nickname/video/:id',
};

export default routes;
